import GeoipSettingsPage from './GeoipSettingsPage';
import { components as commonComponents } from '../../common/components';

export const components = {
  ...commonComponents,
  GeoipSettingsPage,
};
